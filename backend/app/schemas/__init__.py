"""
schemas package — Pydantic v2 request/response models.

Each module mirrors the corresponding api/ route module.

Modules:
    upload          UploadResponse
    snapshot        SnapshotResponse
    behaviours      BehavioursResponse, BehaviourDetail
    savings         SavingsResponse, SavingOpportunity
    simulation      SimulationRequest, SimulationResponse
    coach           CoachRequest, CoachResponse
    plan            PlanResponse, PlanItem
    common          Shared base models and enums
"""
